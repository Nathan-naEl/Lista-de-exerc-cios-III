#include <stdio.h>

int main() {
    int n;
    printf("Digite um numero: ");
    if (scanf("%d", &n) != 1) return 0;
    if (n > 0) printf("Positivo\n");
    else if (n < 0) printf("Negativo\n");
    else printf("Zero\n");
    return 0;
}
