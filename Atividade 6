#include <stdio.h>

int main() {
    int v[10], i, j, cont;
    for(i=0;i<10;i++) {
        printf("v[%d]: ", i);
        scanf("%d", &v[i]);
    }
    printf("Numeros que nao se repetem: ");
    for(i=0;i<10;i++) {
        cont = 0;
        for(j=0;j<10;j++) if (v[j]==v[i]) cont++;
        if (cont==1) printf("%d ", v[i]);
    }
    printf("\n");
    return 0;
}
