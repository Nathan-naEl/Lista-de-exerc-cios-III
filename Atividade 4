#include <stdio.h>

int main() {
    long long n;
    int cont = 0;
    printf("Digite um numero inteiro positivo: ");
    if (scanf("%lld", &n) != 1) return 0;
    if (n == 0) cont = 1;
    while (n != 0) { cont++; n /= 10; }
    printf("%d digitos\n", cont);
    return 0;
}
