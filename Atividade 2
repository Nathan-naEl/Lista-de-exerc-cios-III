#include <stdio.h>
#include <stdlib.h>

int main() {
    int a, b, i;
    printf("Digite dois numeros inteiros: ");
    if (scanf("%d %d", &a, &b) != 2) return 0;
    if (a > b) {
        int t = a; a = b; b = t;
    }
    for(i = a; i <= b; i++) printf("%d ", i);
    printf("\n");
    return 0;
}
